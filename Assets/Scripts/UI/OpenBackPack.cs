using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenBackPack : MonoBehaviour
{
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.M))
        {
            //BackPackUI.Instance.ChangeActive();
        }
    }
}
